# 釣果予測システム (Fishing Catch Predictor)

機械学習を使って「明日釣りに行くべきか」を判断するAWS Lambda関数のコードを管理するリポジトリ

> **このリポジトリについて**
> モデルを学習するコードと釣果データをWebから取得するコードは含まれません。

## 📁 プロジェクト構成

```
fishing-catch-predictor/
├── src/
│   ├── predictor/              # 釣果予測
│   │   ├── __init__.py
│   │   ├── features.py         # 特徴量生成
│   │   ├── data_loader.py      # S3データ読み込み
│   │   └── predictor.py        # 予測ロジック
│   │   └── handler.py          # 予測ハンドラー
│   │
│   └── data_updater/           # データ更新
│       ├── __init__.py
│       ├── updater.py          # 日次更新ハンドラー
│       └── initial_setup.py    # 初回セットアップハンドラー
├── .gitignore
└── README.md
```

## 🎯 Lambda関数

| 関数名 | 目的 | ハンドラー |
|--------|------|-----------|
| **fishing-catch-predictor** | 翌日釣果の予測 | `src.predictor.handler.lambda_handler` |
| **fishing-data-updater** | 前日分データの追加 | `src.data_updater.updater.lambda_handler` |
| **fishing-data-initial-setup** | 全履歴データの初期取り込み | `src.data_updater.initial_setup.lambda_handler` |
